@using Greeny.BLL.ModelVM.Post
@model List<PostListVM>
@{
    Layout = "_DashBoardLayout";
}
@* <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Posts Dashboard</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:wght@700&display=swap"
          rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="/css/dashboard.css">
</head> *@

@* <body> *@

    <div class="dashboard">

        @* <aside class="sidebar">

            <div class="logo">
                <i class="fa-solid fa-leaf"></i>
                <h2>Greeny</h2>
            </div>

            <nav>

                <a href="dashboard.html">
                    <i class="fa-solid fa-chart-line"></i>
                    Dashboard
                </a>

                <a href="products.html">
                    <i class="fa-solid fa-box"></i>
                    Products
                </a>

                <a href="customers.html">
                    <i class="fa-solid fa-users"></i>
                    Customers
                </a>

                <a href="orders.html">
                    <i class="fa-solid fa-cart-shopping"></i>
                    Orders
                </a>

                <a class="active" href="posts.html">
                    <i class="fa-solid fa-newspaper"></i>
                    Posts
                </a>

            </nav> *@

        @* </aside> *@

        <main class="main-content">

            <header class="topbar">

                <div>
                    <h1>Posts</h1>
                    <p>Manage blog articles and content</p>
                </div>

                @* <button class="add-btn">
                    <i class="fa-solid fa-pen"></i>
                    New Post
                </button> *@

            </header>

            <section class="cards">

                <div class="card">
                    <div class="card-icon posts-icon">
                        <i class="fa-solid fa-newspaper"></i>
                    </div>
                    <div>
                        <h3>Total Posts</h3>
                        <h2>@Model.Count()</h2>
                    </div>
                </div>

                @* <div class="card">
                    <div class="card-icon products-icon">
                        <i class="fa-solid fa-eye"></i>
                    </div>
                    <div>
                        <h3>Views</h3>
                        <h2>45.2K</h2>
                    </div>
                </div> *@

                <div class="card">
                    <div class="card-icon customers-icon">
                        <i class="fa-solid fa-heart"></i>
                    </div>
                    <div>
                        <h3>Votes</h3>
                        <h2>@ViewBag.SumVotes</h2>
                    </div>
                </div>

            </section>

            <section class="table-section">

                <div class="section-header">
                    <h2>Latest Posts</h2>
                </div>

                <table>

                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Title</th>
                            <th>Author</th>
                            <th>Date</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var post in Model)
                        {
                            <tr>
                                <td>#@post.Id</td>
                                <td></td>
                                <td>@post.AuthorName</td>
                                <td>@post.FormattedDate</td>
                                <td>
                                    <form asp-controller="Post" asp-action="Delete" asp-route-id="@post.Id" method="post" onsubmit="return confirm('Are you sure you want to delete this post?');" style="margin: 0; padding: 0; display: inline-flex;">
                                        <button type="submit" style="background: none; border: none; color: #dc3545; cursor: pointer; display: inline-flex; align-items: center; gap: 6px; font-size: 0.85rem; font-weight: 600; padding: 6px 10px; border-radius: 4px; font-family: inherit; transition: background 0.1s;" onmouseover="this.style.backgroundColor='#fff5f5'" onmouseout="this.style.backgroundColor='transparent'">
                                            <i class="fa-regular fa-trash-can"></i>
                                            <span>Delete</span>
                                        </button>
                                    </form>
                                </td>
                                <td>
                                    <form asp-controller="Post" asp-action="Details" asp-route-id="@post.Id" method="get" style="margin: 0; padding: 0; display: inline-flex;">
                                        <button type="submit" style="background: none; border: none; color: green; cursor: pointer; display: inline-flex; align-items: center; gap: 6px; font-size: 0.85rem; font-weight: 600; padding: 6px 10px; border-radius: 4px; font-family: inherit; transition: background 0.1s;" onmouseover="this.style.backgroundColor='#fff5f5'" onmouseout="this.style.backgroundColor='transparent'">
                                            <i class="fa-regular info-label"></i>
                                            <span>Details</span>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>

            </section>

        </main>

    </div>

@* </body>

</html> *@